package __PACKAGE__;

/** Identifies this project; replace with real code. */
public final class ProjectInfo {
    private ProjectInfo() {}

    /** Returns the human-readable project name. */
    public static String name() {
        return "__NAME__";
    }
}
