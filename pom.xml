<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.alienspacebunny</groupId>
    <artifactId>alienspacebunny-build</artifactId>
    <version>0.1.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>AlienSpaceBunny shared build (aggregator)</name>
    <description>Builds, versions and releases the shared parent POM and the build-tools jar it
        references. Not a parent and never deployed: build-tools and parent are standalone POMs.</description>
    <url>https://github.com/AlienSpaceBunny/alienspacebunny-build</url>
    <licenses>
        <license>
            <name>Mozilla Public License 2.0</name>
            <url>https://www.mozilla.org/MPL/2.0/</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <!-- build-tools must build before parent: parent's Checkstyle plugin depends on it. -->
    <modules>
        <module>build-tools</module>
        <module>parent</module>
    </modules>
    <scm>
        <connection>scm:git:https://github.com/AlienSpaceBunny/alienspacebunny-build.git</connection>
        <developerConnection>scm:git:git@github.com:AlienSpaceBunny/alienspacebunny-build.git</developerConnection>
        <tag>HEAD</tag>
        <url>https://github.com/AlienSpaceBunny/alienspacebunny-build</url>
    </scm>
    <properties>
        <!-- CI passes -Dasb.release.push=true; local releases never push. -->
        <asb.release.push>false</asb.release.push>
        <!-- Only parent and build-tools are published. -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>3.2.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>3.2.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>3.22.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>3.3.1</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>versions-maven-plugin</artifactId>
                    <version>2.22.0</version>
                    <configuration>
                        <generateBackupPoms>false</generateBackupPoms>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!--
                Release: `mvn release:prepare release:perform` tags v<version>, installs the
                release to ~/.m2 and moves every module (and parent's asb.build-tools.version)
                to the next -SNAPSHOT. Locally nothing is pushed or deployed; the GitHub
                release workflow pushes and publishes to Maven Central.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <configuration>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                    <tagNameFormat>v@{project.version}</tagNameFormat>
                    <pushChanges>${asb.release.push}</pushChanges>
                    <localCheckout>true</localCheckout>
                    <goals>install</goals>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
